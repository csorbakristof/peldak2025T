<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:TemperatureMonitoring.Model"
             x:Class="TemperatureMonitoring.MainPage">

    <VerticalStackLayout Padding="30,0" Spacing="25">
        <Button Text="Load CSV" Clicked="LoadCsv_Clicked" HorizontalOptions="Fill" />
        <CollectionView ItemsSource="{x:Binding TempHumList}" VerticalScrollBarVisibility="Always" HeightRequest="200">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:TempHum">
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Time:" />
                        <Label Text="{x:Binding Time}" />
                        <Label Text="T:" />
                        <Label Text="{x:Binding Temperature}" />
                        <Label Text="RH:" />
                        <Label Text="{x:Binding Humidity}" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
